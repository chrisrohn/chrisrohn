{
  "name": "chrisrohn-new-music-site",
  "private": true,
  "description": "Browser smoke test for chrisrohn.com (site/) — run with `npm test`",
  "scripts": {
    "test": "playwright test",
    "serve": "python3 -m http.server -d site 8000"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0"
  }
}
